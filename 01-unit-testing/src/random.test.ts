import { expect, it } from "vitest";
import { getRandomNumber } from "./random";

it.todo("generates a random number between 1-10", () => {
});

it.todo("generates a random number between 1-50", () => {
});

it.todo("generates 10 random numbers between 1-20", () => {
});
